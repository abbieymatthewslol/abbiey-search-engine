[pytest]
# Windows: global pyreadline can break on Python 3.12 (collections.Callable), which
# crashes pytest while loading the capture plugin. This suite does not use capsys/capfd.
addopts = -p no:capture
