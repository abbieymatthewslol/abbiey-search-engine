# Node (developer CLI under cli/)
cli/node_modules/

__pycache__/
.pytest_cache/
*.pyc
.env
venv/
.venv/
dist/
*.egg-info/
*.db
# Vercel CLI: commit project link only; never commit pulled env (see vercel pull)
.vercel/*
!.vercel/project.json
.env*.local
.env.local

# Encrypted local secrets (scripts/store-vercel-token.ps1 — DPAPI; never commit)
.local/
