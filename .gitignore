__pycache__/
*.pyc
.env
venv/
.venv/
dist/
*.egg-info/
*.db
# Vercel CLI: never commit link metadata or pulled env (use `vercel link` locally)
.vercel/
.env*.local

# Vercel local env
.env.local
.env*.local

# IDE / agent local config (not part of the shipped app)
.cursor/
.vscode/
.agents/

# Accidental runtime / transcript junk
ter
verify_log.txt
